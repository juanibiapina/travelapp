<% content_for :title, @trip.name %>
<% content_for :header do %>
  <%= render "header_with_tabs" %>
<% end %>

<div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="bg-white rounded-lg shadow">
    <div class="px-4 py-5 sm:p-6">
      <div class="mb-6">
        <h2 class="text-lg font-medium text-gray-900">Trip Overview</h2>
        <p class="mt-1 text-sm text-gray-600">General information about your trip</p>
      </div>

      <%= render @trip %>
    </div>
  </div>
</div>
