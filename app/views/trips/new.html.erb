<% content_for :title, "New trip" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-8">
    <div class="flex items-center mb-4">
      <div class="flex-shrink-0">
        <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
          <%= heroicon "plus", options: { class: "w-6 h-6 text-blue-600" } %>
        </div>
      </div>
      <div class="ml-4">
        <h1 class="text-3xl font-bold text-gray-900">Create New Trip</h1>
        <p class="mt-1 text-lg text-gray-600">Start organizing your next adventure</p>
      </div>
    </div>
  </div>

  <div class="bg-white p-8 rounded-xl shadow-sm border border-gray-200">
    <%= render "form", trip: @trip %>
  </div>

  <div class="mt-6 flex items-center">
    <%= link_to trips_path, class: "inline-flex items-center text-blue-600 hover:text-blue-800 font-medium transition-colors duration-200" do %>
      <%= heroicon "arrow-left", options: { class: "w-4 h-4 mr-2" } %>
      Back to Trips
    <% end %>
  </div>
</div>
